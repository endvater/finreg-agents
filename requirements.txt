# FinRegAgents v2 – Core Dependencies
#
# Empfohlene Runtime: Python 3.12
# (3.13 kann bei einzelnen Embedding-Backends zu Inkompatibilitäten führen)
#
# Basis-Installation (Anthropic + lokale Embeddings, kein OpenAI-Key nötig):
#   pip install -r requirements.txt
#
# Optionale Provider-Extras:
#   pip install -r requirements-openai.txt   # OpenAI GPT-4o + Embeddings
#   pip install -r requirements-gemini.txt   # Google Gemini + Embeddings
#   pip install -r requirements-mistral.txt  # Mistral Large + Embeddings
#   pip install -r requirements-cohere.txt   # Cohere Command R+
#   pip install -r requirements-grok.txt     # xAI Grok-3 (OpenAI-kompatibel)
#   pip install -r requirements-ollama.txt   # Ollama lokal + Embeddings
#   pip install -r requirements-all.txt      # Alle Provider

# ── LlamaIndex Core (Retrieval + Index) ──────────────────────────────
llama-index-core>=0.11.0
llama-index-readers-file>=0.2.0

# ── LangChain Core ────────────────────────────────────────────────────
langchain-core>=0.3.0

# ── Anthropic (Standard-Provider) ────────────────────────────────────
langchain-anthropic>=0.3.0

# ── Lokale Embeddings (Fallback, kein API-Key nötig) ─────────────────
llama-index-embeddings-fastembed>=0.2.0
fastembed>=0.3.0,<0.8.0  # 0.8.0+ nicht verfügbar für Python 3.12

# ── Dokumenten-Verarbeitung ───────────────────────────────────────────
pypdf>=4.0.0
pandas>=2.0.0
openpyxl>=3.1.0

# ── Sicherheits-Pins (CVE-Fixes) ──────────────────────────────────────
# TODO: CVE-2026-25990 erfordert pillow>=12.1.1, aber fastembed<0.8.0 erfordert pillow<12.0
# Entferne die <12.0 Beschränkung wenn fastembed 0.8.0+ verfügbar ist
pillow>=10.3.0,<12.0.0

# ── Utilities ─────────────────────────────────────────────────────────
python-dotenv>=1.0.0
pyyaml>=6.0
tqdm>=4.66.0

# ── Testing & Linting ─────────────────────────────────────────────────
pytest>=8.0.0
ruff>=0.4.0
streamlit>=1.30.0
